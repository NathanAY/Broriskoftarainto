There is a test that creates an Item that applies a buff.
It has one WARNING: Detected 1 orphan nodes.  <Node> Id:150961391423
The Debugger, ObjectDb profiler shows that the orphan node is a node of type Buff with that has script buff.gd

You can run this test by next command ".\run_tests_gdunit_custom.bat Systems/Items/test_stat_creation_buff.gd"

=======
There is a test test/Systems/Items/test_buff_in_combat.gd

The test verify that the buff item is working.
By default the buff should increase the attack_speed stat when on_hit event occurs.
I see in the ui that the buff stacks, so the event occurs.
The problem is that the character has no stat changes, so the buff do nothing by some reason.
Fix that problem, make the buff working.

You can run this test by next command ".\run_tests_gdunit_custom.bat Systems/Items/test_buff_in_combat.gd"

======
There is a test that working fine when I run it in test/Systems/Items/test_buff_in_combat.gd

The test verify that the buff item is working.
By default the buff should increase the attack_speed stat when on_hit event occurs.
I see in the ui that the buff stacks, so the event occurs.
The problem is that the character has no stat changes, so the buff do nothing by some reason.
Fix that problem, make the buff working.

You can run this test by next command ".\run_tests_gdunit_custom.bat Systems/Items/test_buff_in_combat.gd"
